NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Optional. Server-only. Add before locking down the public API RPC.
# SUPABASE_SERVICE_ROLE_KEY=
# GitHub OAuth (read-only). Token is used once for a scan and never stored.
# Callback: https://<host>/api/github/callback
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=
